stages:
  - build
  - deploy

build:
  stage: build
  image: node:18
  script:
    - npm ci
    - npm run build
    - echo "Build output contents:"
    - ls -la dist
  variables:
    VITE_API_URL: ${VITE_API_URL}
  artifacts:
    paths:
      - dist/
    expire_in: 1 week

deploy:
  stage: deploy
  image: alpine:latest
  needs:
    - build
  before_script:
    - apk add --no-cache openssh git
    - mkdir -p ~/.ssh
    - echo "$SSH_KNOWN_HOSTS" > ~/.ssh/known_hosts
    - eval $(ssh-agent -s)
    # Load the deploy key from the remote machine
    - ssh -o StrictHostKeyChecking=no $SSH_USER@$SSH_HOST "cat ~/.ssh/deploy_key_jobsweep_ui" | ssh-add -
  script:
    # First, pull the latest changes on the remote server
    - ssh -o StrictHostKeyChecking=no $SSH_USER@$SSH_HOST "cd /home/dev/client && git pull origin staging"
    
    # Ensure the target directory exists and is empty
    - ssh -o StrictHostKeyChecking=no $SSH_USER@$SSH_HOST "mkdir -p /var/www/client && rm -rf /var/www/client/*"
    
    # Then copy the files
    - scp -r dist/* $SSH_USER@$SSH_HOST:/var/www/client/
  only:
    - staging





